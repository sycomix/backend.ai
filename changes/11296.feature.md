Add the `AppConfigFragment` service / adapter / DTO surface — bulk-only writes (admin + self-service) with partial-success semantics, scope-bound + admin search reads. Layers on top of the foundation in #11282 (BEP-1052 §3 / §4 / §5).
