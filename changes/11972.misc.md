Fix the HA (Sentinel) development halfstack created with `--configure-ha` not working on macOS. The Sentinels now announce the master/replica by hostname (`announce-hostnames yes`) so the host-side manager/agent can reach the master, which requires the `/etc/hosts` entries for `node01`-`node03`.
